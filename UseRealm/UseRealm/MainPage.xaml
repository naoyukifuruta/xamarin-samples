<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UseRealm.MainPage">

    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness"
            iOS="0,48,0,0" />
    </ContentPage.Padding>
    
    <StackLayout Orientation="Vertical">
        <StackLayout Orientation="Horizontal">
            <Label Text="Welcome to Xamarin Forms!" HorizontalOptions="Start" VerticalTextAlignment="Center" Margin="8,0,0,0"/>
            <Button Text="Add" Clicked="AddAction" HorizontalOptions="EndAndExpand" Margin="0,0,16,0"/>
        </StackLayout>
        <ContentView HeightRequest="1" BackgroundColor="DarkGray" />
        <ListView x:Name="listView" />
    </StackLayout>

</ContentPage>
